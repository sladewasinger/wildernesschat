# Legacy WFC Modules

These files are kept as a historical prototype reference only.

- Not used by the current runtime entrypoint (`src/main.ts`)
- Not part of the active architecture for the organic village generator

They may be removed after the new generator fully replaces all legacy paths.
